第一版说明：该版本为中期结束版本，目前，基本版本中的设定为不使用voronoi图；已经写了部分快速通信的算法，但是处于关闭状态，即按照目前打开的代码状态，全部为中期版本结束状态

利用快速算法的关键是，利用中间变量L作为整个交互过程中的处理量，只有com_map的值，始终保持正确；exist_map的值是不准确的，只有在进行贝叶斯定理更新的时候才会用到存在概率，但是仅仅
对搜索范围内的进行更新。所以在计算reward的时候也要注意exist_map的使用